interface:
  display_name: "Update llama.cpp"
  short_description: "Update llama.cpp, bindings, and server"
  default_prompt: "Use $update-llamacpp to update vendored llama.cpp, sync Cython bindings and the Python server wrapper, then build and test xllamacpp."
